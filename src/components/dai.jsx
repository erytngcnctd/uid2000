import React, { Component, useContext, useState } from 'react'
import { createClient, useSubscription } from 'urql'
import { UngrundContext } from '../context/UngrundContext'

export const Dai = ({ id, royalties, creator }) => {
    return (
        <div></div>
    )
}